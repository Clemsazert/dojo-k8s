apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cleaner.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "cleaner.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "cleaner.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "task-manager/cleaner:{{ .Values.image.tag }}"
          imagePullPolicy: Never
          envFrom:
            - configMapRef:
                name: {{ include "cleaner.name" . }}
