apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "worker.name" . }}
  labels:
    {{- include "worker.labels" . | nindent 4 }}
data:
  {{- with .Values.env }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
